#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
int r,l,m,i,flag=0;
printf("Enter  number:");
    scanf("%d",&r);
    printf("Enter starting limit:");
    scanf("%d",&l);
    printf("Enter  ending limit:");
    scanf("%d",&m);
    for(i=l;i<=m;i++)
    {
        if(n==i)
        {
            flag=1;
        }
    }
    if(flag==1)
    {
        printf("Yes");
    }
    else
    {
        printf("No");
    }
    getch();
}
